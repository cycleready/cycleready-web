<h1>Bike Routes</h1>
<p>
this is where new routes can be submitted
</p>

<%= form_for :bikeroute, url: bikeroutes_path do |f| %>
<% if @bikeroute.errors.any? %>
 <div id="error_explanation">
   <h2>
     <%= pluralize(@bikeroute.errors.count, "error") %> prohibited
     this article from being saved:
   </h2>
   <ul>
     <% @bikeroute.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
   </ul>
 </div>
<% end %>




  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', bikeroutes_path %>
